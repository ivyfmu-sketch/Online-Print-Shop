'use client';
import { useEffect,useState } from 'react';
import { api } from '../../lib/api';
export default function Admin(){const[orders,setOrders]=useState([]);const[metrics,setMetrics]=useState({});const[settings,setSettings]=useState({mono_price_per_page:'2',color_price_per_page:'5',upi_id:'merchant@upi'});useEffect(()=>{api('/admin/dashboard').then(d=>setMetrics(d.metrics));api('/admin/orders').then(d=>setOrders(d.orders)).catch(()=>location.href='/login')},[]);async function save(e){e.preventDefault();await api('/admin/settings',{method:'PUT',body:JSON.stringify(settings)});alert('Saved')}return <main className="container"><h1>Admin dashboard</h1><section className="grid"><div className="card"><p>Orders</p><div className="stat">{metrics.orders||0}</div></div><div className="card"><p>Revenue</p><div className="stat">₹{metrics.revenue||0}</div></div><div className="card"><p>Completed</p><div className="stat">{metrics.completed||0}</div></div></section><section className="grid two"><form className="form panel" onSubmit={save}><h2>Price & payment settings</h2><input placeholder="Mono price" value={settings.mono_price_per_page} onChange={e=>setSettings({...settings,mono_price_per_page:e.target.value})}/><input placeholder="Color price" value={settings.color_price_per_page} onChange={e=>setSettings({...settings,color_price_per_page:e.target.value})}/><input placeholder="UPI ID" value={settings.upi_id} onChange={e=>setSettings({...settings,upi_id:e.target.value})}/><button>Save settings</button></form><div className="panel"><h2>Printer management</h2><p>Configure DEFAULT_PRINTER and PRINTER_COMMAND on the admin machine. Supported OS commands include CUPS lp for Linux/macOS and custom Windows wrappers.</p><button onClick={()=>api('/admin/printers/test',{method:'POST'}).then(()=>alert('Test queued'))}>Test print</button></div></section><h2>Order management</h2><table className="table panel"><thead><tr><th>Order</th><th>File metadata</th><th>User</th><th>Type</th><th>Copies</th><th>Amount</th><th>Status</th><th>Actions</th></tr></thead><tbody>{orders.map(o=><tr key={o.id}><td>{o.id.slice(0,8)}</td><td>{o.original_name} · {o.page_count}p · {o.file_size} bytes</td><td>{o.user_name}</td><td>{o.print_type}</td><td>{o.copies}</td><td>₹{o.amount}</td><td>{o.status}</td><td><button onClick={()=>api(`/admin/orders/${o.id}/reprint`,{method:'POST'})}>Reprint</button> <button className="secondary" onClick={()=>api(`/admin/orders/${o.id}/cancel`,{method:'POST'})}>Cancel</button></td></tr>)}</tbody></table></main>}
