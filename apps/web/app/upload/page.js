'use client';
import { useState } from 'react';
import { api } from '../../lib/api';
export default function Upload(){const[file,setFile]=useState(null);const[preview,setPreview]=useState('');const[msg,setMsg]=useState('');function pick(e){const f=e.target.files[0];setFile(f);setPreview(f?URL.createObjectURL(f):'')}async function submit(e){e.preventDefault();const fd=new FormData();fd.append('document',file);try{const {document}=await api('/uploads',{method:'POST',body:fd});sessionStorage.setItem('document',JSON.stringify(document));location.href='/print-settings'}catch(err){setMsg(err.message)}}return <main className="container"><h1>Upload document</h1><form className="form panel" onSubmit={submit}><input type="file" accept="application/pdf,image/png,image/jpeg" required onChange={pick}/>{preview&& (file?.type==='application/pdf'?<iframe className="preview" src={preview}/>:<img className="preview" src={preview} alt="preview"/>)}<button disabled={!file}>Continue to print settings</button>{msg&&<p>{msg}</p>}</form></main>}
