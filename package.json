{
  "name": "online-print-shop",
  "version": "1.0.0",
  "private": true,
  "description": "Secure online print shop with user/admin dashboards, UPI payments, and printer automation.",
  "workspaces": ["apps/*", "packages/*"],
  "scripts": {
    "dev": "concurrently \"npm run dev --workspace apps/api\" \"npm run dev --workspace apps/web\"",
    "check": "npm run check --workspace apps/api && npm run check --workspace apps/web"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  },
  "engines": { "node": ">=20" }
}
